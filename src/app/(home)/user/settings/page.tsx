import Layout from "@/components/Layout/Layout";
import SettingsPage from "@/components/Pages/SettingsPage";

const page = () => {
	return (
		<Layout>
			<SettingsPage />
		</Layout>
	);
};

export default page;
